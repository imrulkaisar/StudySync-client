/**
 *
 * Requirements: 
 * ========================
 * Assignment Deleting: 
 * 1.  Only the user is able to delete an assignment who has created the assignment. A user is not able to delete assignments which are created by other users .
 * 2. A successful message will be shown when the assignment will be deleted successfully.
 * 3. An error message will be shown when a user tries to delete an assignment which is not created by himself …
 * 


 */

const AssignmentArchive = () => {
  return (
    <div>
      <h2>Assignment Archive</h2>
    </div>
  );
};

export default AssignmentArchive;
